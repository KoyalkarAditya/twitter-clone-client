/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "pbs.twimg.com",
      "lh3.googleusercontent.com",
      "twitter-image.s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
